#!/bin/env bash

net="p_net"
name="game123"

# project location
project="$HOME/scripts"

docker run -it --rm \
    --name "$name" \
    --network "$net" \
    -v /var/run/docker.sock:/var/run/docker.sock \
    -v "$project":/project \
    docker_game
