# module's

# declared import module, to locate your module, 
# need to specifically say where the module is located
# import "leatep/hello"
# - go mod edit -replace leatep/hello=../hello *(where hello folder is)
# and then 
# - go mod tidy  # so he locate the module

# go slice = (list/array)
# - name[string] (list of strings or anything)

# go map = (dict / json but not quite)
# map[string]string (map of string keys, and string values)

