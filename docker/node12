sudo docker run -dp 3000:3000 \
    -w /app \
    -v "/home/leatep/Desktop/code/docker/docker_app:/app" \
    --network wtf \
    -e MYSQL_HOST=mysql \
    -e MYSQL_USER=root \
    -e MYSQL_PASSWORD=secret \
    -e MYSQL_DB=wtf \
    node:12-alpine \
    sh -c "yarn install && yarn run dev"